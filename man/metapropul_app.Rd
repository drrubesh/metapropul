% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gui.R
\name{metapropul_app}
\alias{metapropul_app}
\title{Launch the metapropul graphical interface}
\usage{
metapropul_app(
  launch.browser = getOption("viewer", TRUE),
  host = getOption("shiny.host", "127.0.0.1"),
  port = getOption("shiny.port"),
  ...
)
}
\arguments{
\item{launch.browser}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}}. By default, the RStudio
Viewer is used when available; otherwise the system browser opens. Use
\code{FALSE} when embedding or testing the app.}

\item{host}{Host address on which to serve the application.}

\item{port}{Optional TCP port. When \code{NULL}, Shiny chooses an available port.}

\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Called for its side effect. The return value from
\code{\link[shiny:runApp]{shiny::runApp()}} is returned invisibly when the application stops.
}
\description{
Starts an interactive Shiny application for importing study-level data,
configuring a meta-analysis, reviewing the pooled results, and exporting
tables and forest plots. The graphical interface calls the same exported
analysis and reporting functions used in an R script; it does not implement
a separate statistical engine.
}
\details{
The app supports analyses of proportions, ratios, continuous outcomes,
generic inverse-variance effects, correlations, and incidence rates. Bundled
example datasets are available from the data panel, and users can upload a
CSV or Excel workbook for their own analysis.

\code{shiny} and \code{bslib} are optional dependencies because they are required only
for the graphical interface. The optional \code{readxl} package enables Excel
imports. Install them with
\code{install.packages(c("shiny", "bslib", "readxl"))} if needed.
}
\examples{
\dontrun{
metapropul_app()
}

}
