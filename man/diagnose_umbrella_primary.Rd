% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umbrella_diagnostics.R
\name{diagnose_umbrella_primary}
\alias{diagnose_umbrella_primary}
\title{Diagnose primary-study evidence within each source meta-analysis}
\usage{
diagnose_umbrella_primary(
  data,
  outcome,
  review,
  study,
  effect,
  lower,
  upper,
  participants = NULL,
  effect_scale = c("ratio", "identity"),
  ci_level = 0.95,
  method = "REML",
  alpha = 0.05,
  expected_effect = c("largest", "reported"),
  reported_effect = NULL,
  min_egger = 10L,
  duplicate_action = c("error", "precision", "first"),
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{data}{Long-format primary-study estimates.}

\item{outcome, review, study}{Columns identifying outcome, source review, and study.}

\item{effect, lower, upper}{Estimate and confidence-limit columns.}

\item{participants}{Optional participant-count column used to identify the largest study.}

\item{effect_scale}{\code{"ratio"} or \code{"identity"}.}

\item{ci_level}{Confidence level of supplied intervals.}

\item{method}{Tau-squared estimator passed to \code{\link[metafor:rma]{metafor::rma()}}.}

\item{alpha}{Significance threshold.}

\item{expected_effect}{\code{"largest"} or \code{"reported"}. For \code{"reported"}, supply
\code{reported_effect} as a column containing the source meta-analysis estimate.}

\item{reported_effect}{Optional reported pooled-effect column.}

\item{min_egger}{Minimum primary studies for Egger testing.}

\item{duplicate_action}{Handling of conflicting duplicate rows within the
same review: \code{"error"}, \code{"precision"}, or \code{"first"}.}

\item{tolerance}{Numerical equivalence tolerance.}
}
\value{
An \code{umbrella_primary_diagnostics} object. Its summary has one row per
source review result and can be passed to \code{\link[=classify_umbrella]{classify_umbrella()}}.
}
\description{
Reconstructs diagnostics separately for every outcome--review combination.
It never merges primary studies from different reviews into a new estimate.
}
\section{CSV and Excel columns}{

Use one row per primary-study estimate within a source review. Outcome,
review, study, effect, lower limit, and upper limit are required;
participants are optional. Every outcome--review group must contain at
least two unique primary studies. Studies from different reviews are never
combined into a new pooled estimate.
}
