% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_rob.R
\name{forest_rob}
\alias{forest_rob}
\title{Combined forest plot and risk-of-bias traffic lights}
\usage{
forest_rob(
  x,
  rob_data,
  studylab,
  tool = "GENERIC",
  domains = NULL,
  overall = NULL,
  levels = NULL,
  colours = NULL,
  has_overall = TRUE,
  title = NULL,
  new = FALSE,
  ...
)
}
\arguments{
\item{x}{A supported metapropul meta-analysis object.}

\item{rob_data}{A data frame containing one row per study and risk-of-bias
judgements in separate columns.}

\item{studylab}{Character string naming the study-label column in
\code{rob_data}. Labels must match the fitted study labels in \code{x}.}

\item{tool}{Character string specifying the appraisal tool. See
\code{\link[=plot_rob]{plot_rob()}} for supported values.}

\item{domains}{Character vector naming the judgement columns. When an
overall column is included, identify it with \code{overall}.}

\item{overall}{Optional character string naming the overall-judgement
column. If \code{NULL}, a column in \code{domains} named \code{overall} (ignoring case)
is detected automatically.}

\item{levels}{Optional allowed judgement levels for \code{tool = "Custom"}.}

\item{colours}{Optional named colours for \code{tool = "Custom"}.}

\item{has_overall}{Logical indicating whether a custom tool has an overall
judgement.}

\item{title}{Optional plot title. No title is drawn by default.}

\item{new}{Logical passed to the forest engine. The default \code{FALSE} avoids
an empty first page when a combined plot is written to a file device.}

\item{...}{Further arguments passed to \code{\link[=forest_meta]{forest_meta()}}, including
\code{save_as}, \code{filename}, \code{width}, and \code{height}.}
}
\value{
Invisibly returns \code{TRUE}, as does \code{\link[=forest_meta]{forest_meta()}}.
}
\description{
Adds a study-level risk-of-bias table to a fitted meta-analysis and draws
the forest estimates and coloured domain judgements in one aligned figure.
This is useful for review manuscripts in which readers need to compare each
study's effect estimate with its methodological limitations.
}
\details{
Study labels are matched exactly and reordered internally to the fitted
meta-analysis. Missing fitted studies and duplicate labels are errors,
because either condition could place a judgement beside the wrong estimate.
Extra rows in \code{rob_data} are ignored with a warning. Up to ten non-overall
domains are supported by the underlying forest-plot engine.
}
\examples{
\donttest{
data(dat_bcg, package = "metapropul")
fit <- meta_prop(dat_bcg, "tpos", "npos", "author")
labels <- fit$meta$studlab
rob <- data.frame(
  study = labels,
  randomization = rep(c("Low", "Some concerns"), length.out = length(labels)),
  reporting = rep(c("Low", "High"), length.out = length(labels)),
  overall = rep(c("Low", "High"), length.out = length(labels))
)
forest_rob(
  fit, rob, studylab = "study", tool = "Custom",
  domains = c("randomization", "reporting", "overall"),
  levels = c("Low", "Some concerns", "High")
)
}

}
