% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_reg.R
\name{meta_reg}
\alias{meta_reg}
\title{Meta-regression}
\usage{
meta_reg(meta_object, data, moderators, studylab)
}
\arguments{
\item{meta_object}{A fitted object from \code{meta_ratio()},
\code{meta_mean()}, or \code{meta_prop()}.}

\item{data}{The original dataset used to fit the meta-analysis model.}

\item{moderators}{A formula specifying the moderators
(e.g. \code{~ age + region}).}

\item{studylab}{Character string naming the study label column in
\code{data}. This is required so studies are matched safely by
label.}
}
\value{
An object of class \code{"meta_reg"}.
}
\description{
Performs meta-regression on a fitted \code{meta_ratio()},
\code{meta_mean()}, or \code{meta_prop()} object using
\code{metafor::rma()}.
}
\details{
Coefficients are reported on the model scale and, where appropriate,
moderator effects are additionally shown on a back-transformed scale.
For ratio and proportion models, the intercept is not
back-transformed by default because it is often not interpretable
unless continuous moderators are centered.
}
\examples{
\donttest{
data(dat_bcg, package = "metapropul")
result <- meta_prop(
  data = dat_bcg,
  event = "tpos",
  n = "npos",
  studylab = "author"
)
reg <- meta_reg(
  meta_object = result,
  data = dat_bcg,
  moderators = ~ ablat,
  studylab = "author"
)
summary(reg)
}

}
