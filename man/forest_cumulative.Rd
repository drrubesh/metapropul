% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_cumulative.R
\name{forest_cumulative}
\alias{forest_cumulative}
\title{Cumulative forest plot}
\usage{
forest_cumulative(
  object,
  title = NULL,
  layout = "RevMan5",
  prediction = FALSE,
  overall = TRUE,
  save_as = c("viewer", "pdf", "png", "tiff"),
  filename = NULL,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{meta_ratio}, \code{meta_mean}, or \code{meta_prop}
object.}

\item{title}{Optional character string printed above the plot. No title is
drawn when \code{NULL} (the default).}

\item{layout}{Forest plot layout passed to \code{meta::forest()}. One of
\code{"RevMan5"} (default), \code{"JAMA"}, \code{"BMJ"}, or
\code{"meta"}.}

\item{prediction}{Logical; show a prediction interval (default \code{FALSE}).}

\item{overall}{Logical; show the overall cumulative estimate (default
\code{TRUE}).}

\item{save_as}{One of \code{"viewer"} (default), \code{"pdf"},
\code{"png"}, or \code{"tiff"}.}

\item{filename}{Optional file path. If \code{NULL} and
\code{save_as != "viewer"}, a timestamped file is created in
\code{tempdir()}.}

\item{width}{Optional plot width in inches (overrides auto-sizing).}

\item{height}{Optional plot height in inches (overrides auto-sizing).}

\item{...}{Additional arguments passed to \code{meta::forest()}.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Produces a cumulative forest plot from a \code{meta_ratio},
\code{meta_mean}, or \code{meta_prop} object. Each row shows the pooled
estimate after sequentially adding one more study in the order supplied.
}
\details{
\strong{Study ordering:} Sort your data by publication year before fitting
to show when evidence first became conclusive:
\preformatted{
dat <- dat[order(dat$year), ]
result <- meta_ratio(data = dat, ...)
forest_cumulative(result)
}
}
\examples{
\donttest{
data(dat_bcg, package = "metapropul")
dat_bcg <- dat_bcg[order(dat_bcg$year), ]
result <- meta_prop(
  data = dat_bcg, event = "tpos", n = "npos",
  studylab = "author"
)
forest_cumulative(result)
forest_cumulative(result, title = "BCG vaccine -- cumulative evidence")
}

}
